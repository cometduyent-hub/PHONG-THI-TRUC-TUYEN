"use client";
import {useState} from "react";
export default function Student(){const [started,setStarted]=useState(false);const [code,setCode]=useState("");return <main className="login"><div className="card login-card">{!started?<><div className="brand"><div className="brandmark">🎓</div>KHTN SMART TEST</div><h1>Học sinh vào thi</h1><p className="muted">Nhập mã kỳ thi do giáo viên cung cấp.</p><div className="form"><div><label className="label">Mã kỳ thi</label><input className="input" value={code} onChange={e=>setCode(e.target.value)} placeholder="VD: KHTN8-2026"/></div><button className="btn btn-primary" onClick={()=>setStarted(true)} disabled={!code}>Vào phòng thi</button></div></>:<div><h1>Phòng thi</h1><p>Mã: <b>{code}</b></p><div className="notice">V1 mới là khung giao diện. V4 sẽ kết nối kỳ thi thực, đồng hồ, tự lưu và chấm tự động.</div></div>}</div></main>}
